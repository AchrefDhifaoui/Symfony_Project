<form method="post" action="{{ path('app_formation_assuree_delete', {'id': formation_assuree.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation_assuree.id) }}">
    <button class="btn">Delete</button>
</form>
