{% extends 'template.html.twig' %}

{% block title %}Formation{% endblock %}

{% block body %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ formation.Titre }}</td>
            </tr>
            <tr>
                <th>SousTitre</th>
                <td>{{ formation.sousTitre }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ formation.detail }}</td>
            </tr>
            <tr>
                <th>Objectifs</th>
                <td>{{ formation.objectifs }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ formation.contenu }}</td>
            </tr>
            <tr>
                <th>Mode</th>
                <td>{{ formation.mode }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ formation.duree }}</td>
            </tr>
            <tr>
                <th>PrixUnitaire</th>
                <td>{{ formation.prixUnitaire }}</td>
            </tr>
            <tr>
                <th>Unite</th>
                <td>{{ formation.unite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formation_index') }}">back to list</a>

    <a href="{{ path('app_formation_edit', {'id': formation.id}) }}">edit</a>

    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
